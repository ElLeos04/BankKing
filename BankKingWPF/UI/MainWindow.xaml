<Window
        x:Class="BankKingWPF.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankKingWPF.UI"
        xmlns:Components="clr-namespace:BankKingWPF.UI.Components"
        xmlns:vm="clr-namespace:BankKingViewModel;assembly=BankKingViewModel"
        mc:Ignorable="d" x:Name="window"
        Title="MainWindow" Height="1080" Width="1920"
        WindowState="Maximized"
        UseLayoutRounding="True"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel, IsDesignTimeCreatable=True}">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </b:EventTrigger>
        <b:EventTrigger EventName="Closing">
            <b:InvokeCommandAction Command="{Binding SaveDataCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <DockPanel>
        <Grid x:Name="SideBar" Background="{DynamicResource PanelBrush}" DockPanel.Dock="Left" Width="215">
            <Border BorderBrush="{DynamicResource DarkAccentBrush}" BorderThickness="0,0,5,0">
                <Grid>
                    <TextBlock Margin="0,30,0,0" HorizontalAlignment="Center" VerticalAlignment="Top"
                           TextWrapping="Wrap" TextAlignment="Center" FontFamily="{DynamicResource AppFontFamily}" FontSize="65" FontWeight="Bold"
                           Foreground="{DynamicResource DarkAccentBrush}">Bank King</TextBlock>


                    <Grid Margin="0,200,0,0">
                        <!-- TODO : Faire le changement de vue
                        <Components:ImageButton ButtonText="Comptes" ImageSource="/Resources/Icons/account.png" VerticalAlignment="Top" Margin="0,150,0,0" Command="{Binding SaveDataCommand}"/>
                        <Components:ImageButton ButtonText="Épargnes" ImageSource="/Resources/Icons/savings.png" VerticalAlignment="Top" Margin="0,247,0,0"/>
                        -->
                        <Components:ImageButton ButtonText="Ajout Catégorie" ImageSource="/Resources/Icons/parameters.png" Margin="0,0,0,0" Command="{Binding AddCategoryCommand}"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="MainPanel">
            <Border Margin="10,10,10,10"
                    BorderBrush="{DynamicResource DarkAccentBrush}" BorderThickness="5" CornerRadius="12">
                <Grid>
                    <Border Name="mask" Background="{DynamicResource PanelBrush}" CornerRadius="9"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" CanContentScroll="False"
                                  Style="{StaticResource FavsScrollViewer}">
                        <ScrollViewer.OpacityMask>
                            <VisualBrush Visual="{Binding ElementName=mask}"/>
                        </ScrollViewer.OpacityMask>

                        <StackPanel Orientation="Horizontal" Margin="10,10,0,10">

                            <ItemsControl x:Name="AccountGrid" ItemsSource="{Binding Accounts}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Components:AcountDetails Margin="0,0,10,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>

                            </ItemsControl>

                            <Components:AddAccountElement Command="{Binding AddAccountCommand}"/>

                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
