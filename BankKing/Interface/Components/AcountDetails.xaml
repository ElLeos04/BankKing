<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankKing.Interface.Components"
             xmlns:Entry="clr-namespace:BankKing.Data.Entry" x:Class="BankKing.Interface.Components.AcountDetails"
             mc:Ignorable="d" Width="300" Height="1030">
    <Border BorderThickness="0" Background="{DynamicResource DarkPanelBrush}" CornerRadius="6,6,6,6">
        <Grid>
            <Grid x:Name="HeaderPanel" Height="280" VerticalAlignment="Top">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,33,0,0"
                           TextWrapping="Wrap" TextAlignment="Center"
                           FontFamily="{DynamicResource AppFontFamily}" FontSize="32" Foreground="{DynamicResource DarkAccentBrush}"><Run Text="Title"/></TextBlock>

                <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="{DynamicResource AppFontFamily}" FontSize="32" Foreground="{DynamicResource DarkAccentBrush}" Margin="0,100,0,0"><Run Text="20 000€"/></TextBlock>
                <Grid Height="60" VerticalAlignment="Bottom" Margin="0,0,0,40">
                    <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="Revenus du mois" VerticalAlignment="Top" FontFamily="{DynamicResource AppFontFamily}" Foreground="{DynamicResource DarkAccentBrush}" FontSize="14"/>
                    <TextBlock HorizontalAlignment="Right" Margin="0,10,10,0" TextWrapping="Wrap" Text="Dépenses du mois" VerticalAlignment="Top" FontFamily="{DynamicResource AppFontFamily}" Foreground="{DynamicResource DarkAccentBrush}" FontSize="14"/>
                    <TextBlock HorizontalAlignment="Left" Margin="10,27,0,0" TextWrapping="Wrap" Text="+ 0,00€" VerticalAlignment="Top" FontFamily="{DynamicResource AppFontFamily}" Foreground="{DynamicResource PositiveValueBrush}" FontSize="14"/>
                    <TextBlock HorizontalAlignment="Right" Margin="0,27,10,0" TextWrapping="Wrap" Text="- 0,00€" VerticalAlignment="Top" FontFamily="{DynamicResource AppFontFamily}" Foreground="{DynamicResource NegativeValueBrush}" FontSize="14"/>
                </Grid>
                <local:CustomButton Loaded="CustomButton_Loaded" Height="25" VerticalAlignment="Bottom" Margin="0,0,0,15" Width="170" ButtonText="Statistiques Détaillées"/>
            </Grid>

            <Grid x:Name="ContentPanel" Margin="0,280,0,110">
                <Border BorderBrush="{DynamicResource LightAccentBrush}" BorderThickness="2" CornerRadius="8,8,8,8" Margin="10,0,10,0">
                    <Grid x:Name="HistoryGridPanel">
                        <Border Name="mask" Background="White" CornerRadius="8"/>

                        <StackPanel x:Name="HistoryPanel" Background="{DynamicResource LightTouchBrush}" Height="{Binding ActualHeight, ElementName=HistoryGridPanel, Mode=OneWay}">
                            <StackPanel.OpacityMask>
                                <VisualBrush Visual="{Binding ElementName=mask}"/>
                            </StackPanel.OpacityMask>
                        </StackPanel>
                    </Grid>

                </Border>
            </Grid>

            <Grid x:Name="FooterPanel" Height="110" VerticalAlignment="Bottom">
                <local:CustomButton HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" ButtonText="Ajouter Transaction"/>
                <local:CustomButton HorizontalAlignment="Center" Margin="0,60,0,0" VerticalAlignment="Top" ButtonText="Ajouter Transaction Récurrente"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
