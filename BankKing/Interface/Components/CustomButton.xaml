<UserControl x:Class="BankKing.Interface.Components.CustomButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankKing.Interface.Components"
             mc:Ignorable="d"
             Width="170" Height="40">
    <Border BorderThickness="2,2,2,2" CornerRadius="6,6,6,6" BorderBrush="{DynamicResource LightAccentBrush}">
        <Grid>
            <Border Name="Mask" Background="White" CornerRadius="5"/>
            <Button BorderBrush="{DynamicResource LightTouchBrush}" Background="{DynamicResource LightTouchBrush}" FontFamily="{DynamicResource AppFontFamily}" FontSize="14">
                <Button.OpacityMask>
                    <VisualBrush Visual="{Binding ElementName=Mask}"/>
                </Button.OpacityMask>
                <TextBlock TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource DarkAccentBrush}"><Run Text="Ajouter Transaction"/></TextBlock>
            </Button>
            
        </Grid>
    </Border>
</UserControl>
