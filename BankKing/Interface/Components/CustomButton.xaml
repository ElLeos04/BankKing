<UserControl x:Class="BankKing.Interface.Components.CustomButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankKing.Interface.Components"
             mc:Ignorable="d"
             Width="170" Height="40">
    <Border BorderThickness="2,2,2,2" CornerRadius="6,6,6,6" BorderBrush="{DynamicResource LightAccentBrush}">
        <Grid>
            <Border Name="Mask" Background="White" CornerRadius="5"/>
            <StackPanel x:Name="stackPanel" Background="{DynamicResource PanelBrush}">
                <StackPanel.OpacityMask>
                    <VisualBrush Visual="{Binding ElementName=Mask}"/>
                </StackPanel.OpacityMask>
                <Button  Width="{Binding ActualWidth, ElementName=stackPanel, Mode=OneWay}" Height="{Binding ActualHeight, ElementName=stackPanel, Mode=OneWay}" BorderBrush="#00707070" Background="#00DDDDDD" FontFamily="{DynamicResource AppFontFamily}" FontSize="14">
                    <TextBlock TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource DarkAccentBrush}">Ajouter Transaction</TextBlock>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
