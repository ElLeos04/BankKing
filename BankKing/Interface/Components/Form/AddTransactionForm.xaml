<UserControl 
            x:Class="BankKing.Interface.Components.Form.AddTransactionForm"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:BankKing.Interface.Components.Form"
            xmlns:viewmodel="clr-namespace:BankKing.ViewModel.Form"
            mc:Ignorable="d" 
            Height="202"
            Style="{StaticResource AddForm}"
            d:DataContext="{d:DesignInstance Type=viewmodel:AddTransactionViewModel, IsDesignTimeCreatable=True}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:InputTextComponent InputName="Nom"
                                  Grid.Row="0" Grid.Column="0" />
        <local:InputNumberComponent InputName="Montant de départ"
                                    Grid.Row="0" Grid.Column="1" />

        <local:InputTextComponent InputName="Nom" InputText="{Binding AccountTitle}"
                                  Grid.Row="1" Grid.Column="0" />
        <local:InputNumberComponent InputName="Montant de départ"
                                    Grid.Row="1" Grid.Column="1" />

    </Grid>

</UserControl>
