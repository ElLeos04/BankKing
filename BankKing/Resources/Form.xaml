<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:BankKing.ViewModel.Form"
                    xmlns:form="clr-namespace:BankKing.Interface.Components.Form">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/ColorTheme.xaml"/>
        <ResourceDictionary Source="/Resources/Button.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate DataType="{x:Type vm:AddAccountViewModel}">
        <form:AddAccountForm />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AddTransactionViewModel}">
        <form:AddTransactionForm />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AddCategoryViewModel}">
        <form:AddCategoryForm />
    </DataTemplate>

    <Style x:Key="AddForm" TargetType="UserControl">
        <Setter Property="Width" Value="600"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="UserControl">
                    <Border Background="White" CornerRadius="12">
                        <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource DarkAccentBrush}"
                                Margin="10,10,10,10" CornerRadius="12" BorderThickness="5">

                            <DockPanel>
                                <StackPanel DockPanel.Dock="Bottom" Margin="0,20,0,10"
                                            Orientation="Horizontal" FlowDirection="RightToLeft">

                                    <Button Style="{StaticResource RoundedButtonStyle}" Margin="20,0,0,0"
                                            Width="120" Height="22" VerticalAlignment="Bottom"
                                            Command="{Binding AcceptCommand}">
                                        <TextBlock Text="Valider" TextAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="{StaticResource DarkAccentBrush}" FontFamily="{StaticResource AppFontFamily}"/>
                                    </Button>

                                    <Button Style="{StaticResource RoundedButtonStyle}" Margin="20,0,0,0"
                                            Width="120" Height="22" VerticalAlignment="Bottom"
                                            Command="{Binding CancelCommand}">
                                        <TextBlock Text="Annuler" TextAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="{StaticResource DarkAccentBrush}" FontFamily="{StaticResource AppFontFamily}"/>
                                    </Button>

                                </StackPanel>

                                <ContentPresenter/>
                            </DockPanel>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StandardControl" TargetType="Control">
        <Setter Property="Width" Value="250"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Background" Value="{StaticResource LightTouchBrush}"/>

        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
        <Setter Property="Foreground" Value="{StaticResource DarkAccentBrush}"/>

        <Setter Property="BorderBrush" Value="{StaticResource LightAccentBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <Style x:Key="TextInput" TargetType="TextBox" BasedOn="{StaticResource StandardControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border" CornerRadius="6"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                      HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RoundedDatePickerTextBoxStyle" TargetType="{x:Type DatePickerTextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                    <Grid>
                        <ScrollViewer x:Name="PART_ContentHost" 
                                      Margin="5,0,0,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Stretch"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DateInput" TargetType="DatePicker" BasedOn="{StaticResource StandardControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <Border x:Name="OuterBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6" 
                            SnapsToDevicePixels="True">
                        <Grid x:Name="PART_Root" Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- The Text Input Part -->
                            <DatePickerTextBox x:Name="PART_TextBox"
                                               Grid.Column="0"
                                               Style="{StaticResource RoundedDatePickerTextBoxStyle}"
                                               Focusable="{TemplateBinding Focusable}"
                                               HorizontalContentAlignment="Stretch"
                                               VerticalContentAlignment="Center" />

                            <!-- The Calendar Button -->
                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Width="25"
                                    Margin="2,0,2,0"
                                    Focusable="False"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Grid Background="Transparent">
                                            <!-- Simple Calendar Icon (Path) -->
                                            <Path Data="M7,10H12V15H7M19,19H5V8H19M19,3H18V1H16V3H8V1H6V3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5A2,2 0 0,0 19,3Z" 
                                                  Fill="#666666" 
                                                  Stretch="Uniform" 
                                                  Width="14" 
                                                  Height="14"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <!-- The Popup Calendar -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=OuterBorder}"
                                   StaysOpen="False" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#888888"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#007ACC"/>
                            <Setter TargetName="OuterBorder" Property="BorderThickness" Value="1.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RoundedComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>

            <Border x:Name="Border" 
                    Grid.ColumnSpan="2"
                    CornerRadius="6"
                    Background="{StaticResource LightTouchBrush}"
                    BorderBrush="{StaticResource LightAccentBrush}"
                    BorderThickness="2" />

            <Path x:Name="Arrow"
                  Grid.Column="1"     
                  Fill="{StaticResource DarkAccentBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#EEE" />
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#EEE" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource StandardControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <!-- References the ToggleButton template above -->
                        <ToggleButton Name="ToggleButton" 
                                      Template="{StaticResource RoundedComboBoxToggleButton}" 
                                      Grid.Column="2" 
                                      Focusable="false"
                                      IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>

                        <ContentPresenter Name="ContentSite"
                                          IsHitTestVisible="False" 
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="10,3,23,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left" />

                        <Popup Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" 
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid Name="DropDown"
                                  SnapsToDevicePixels="True"                
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource LightTouchBrush}"
                                        BorderThickness="1"
                                        BorderBrush="{StaticResource LightAccentBrush}"
                                        CornerRadius="5"/>
                                <!-- Popup Corners -->
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>